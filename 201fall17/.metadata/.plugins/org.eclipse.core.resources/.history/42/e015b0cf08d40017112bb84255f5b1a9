import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;

public class SortByFreqs {
	public String[] sort(String[] data) {
		// fill in code here
		ArrayList <String> mine = new ArrayList<String>()
		for(String a: data) {
			mine.add(a); 
		}
	
	Collections.sort(mine, new Comparator<String>() {
	    @Override

		public int compare(String v, String w) {
			if (Collections.frequency(mine, v) > Collections.frequency(mine, w)) {
				return 1;
			}
			if (Collections.frequency(mine, v) < Collections.frequency(mine, w)) {
				return -1;
			}
			if (Collections.frequency(mine, v) == Collections.frequency(mine, w)) {
				return v.compareTo(w);
			}
			return 0;
		}
}); 
	String [] ret = new String [mine.size()];
	int i = 0; 
	for(String a: mine) {
		ret[i] = a;
		i++;
	}
	return ret; 
	
	}
	}