import java.util.Comparator;

public class SortByFreqs {
	public String[] sort(String[] data) {
		// fill in code here
	}

	public static class Order implements Comparator<Term> { // Comparator used in autocompletes
		public int compare(String v, String w, int[] data) {
			// TODO: Implement WeightOrder.compare
			if (v.myWeight > w.myWeight) {
				return 1;
			}
			if (v.myWeight < w.myWeight) {
				return -1;
			}
			return 0;
		}
	}
}